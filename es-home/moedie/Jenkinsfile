@Library('es-jenkins-shared') _

pipeline {
    agent any

    stages {
        stage('Build es-home container') {
            steps {
                echo 'Building es-home container...'
                // script to build here ...
                
            }
        }
        stage('another testing'){
            steps {
                echo 'testing 222'
            }
        }

        stage('testing echo'){
            steps {
                testEcho()
            }
        }
    }
}